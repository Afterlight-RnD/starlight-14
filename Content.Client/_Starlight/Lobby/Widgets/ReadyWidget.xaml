<widgets:ReadyWidget
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:widgets="clr-namespace:Content.Client._Starlight.Lobby.Widgets"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:lobbyUi="clr-namespace:Content.Client.Lobby.UI">
    <controls:StripeBack>
        <BoxContainer Orientation="Horizontal" SeparationOverride="6" Margin="3 3 3 3">
            <cc:UICommandButton Command="observe" Name="ObserveButton" Access="Protected"
                                Text="{Loc 'ui-lobby-observe-button'}"
                                StyleClasses="ButtonBig"
                                WindowType="{x:Type lobbyUi:ObserveWarningWindow}" />
            <widgets:RoundTimerWidget Name="RoundTimer" Access="Protected"/>
            <Button Name="ReadyButton" Access="Protected" ToggleMode="True"
                    Text="{Loc 'ui-lobby-ready-up-button'}"
                    StyleClasses="ButtonBig" MinWidth="137" />
        </BoxContainer>
    </controls:StripeBack>
</widgets:ReadyWidget>
